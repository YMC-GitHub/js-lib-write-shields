module.exports = {
  //the dir name to save the source code (for dev)
  src: 'src',
  //the dir name to save the test source code (for test)
  test: 'test',
  //the dir name to save the build ouput code by rollup
  dist: 'lib',
}